import { BookingfilterPipe } from './bookingfilter.pipe';

describe('BookingfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BookingfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
